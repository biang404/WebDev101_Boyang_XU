// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://biang404.github.io',
    base: '/WebDev101_Boyang_XU/', // 重要！适配 GitHub Pages 子路径
});
