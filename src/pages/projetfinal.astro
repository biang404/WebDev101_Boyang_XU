---
import BaseLayout from '../layouts/Layout.astro';
const pageTitle = "ClimatTrend - Visualisation des Températures Moyennes";
---

<BaseLayout title={pageTitle}>
  <style>
    .container {
      text-align: center;
      padding: 20px;
      font-family: sans-serif;
    }
    canvas {
      max-width: 800px;
      margin: 20px auto;
    }
    input, button {
      padding: 8px;
      font-size: 16px;
      margin: 10px;
    }

    .weather-container {
      display: flex;
      flex-direction: row;
      justify-content: flex-start;
      gap: 12px;
      overflow-x: auto;
      padding-bottom: 10px;
      margin-top: 20px;
    }

    .weather-container::-webkit-scrollbar {
      height: 6px;
    }
    .weather-container::-webkit-scrollbar-thumb {
      background-color: #ccc;
      border-radius: 4px;
    }

    .weather-card {
      background: #f8f8f8;
      border-radius: 12px;
      padding: 12px;
      text-align: center;
      width: 80px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      font-size: 14px;
      flex: 0 0 auto;
    }

    .weather-card.selected {
      background: #fff5d0;
      border: 2px solid #f0c000;
    }

    .weather-card .day {
      font-weight: bold;
      margin-bottom: 4px;
    }

    .weather-card .icon {
      font-size: 24px;
      margin: 4px 0;
    }

    .weather-card .temps {
      display: flex;
      justify-content: space-between;
      font-size: 13px;
      margin-top: 4px;
    }

    .weather-card .min {
      opacity: 0.5;
    }

    .weather-card .max {
      opacity: 0.9;
    }
  </style>

  <div class="container">
    <h1>🌍 Visualisateur de Tendance du Climat</h1>
    <div>
      <button onclick="switchView('past')">📈 Statistiques des températures passées</button>
      <button onclick="switchView('forecast')">🌤️ Météo actuelle et prévisions sur 5 jours</button>
    </div>

    <!-- Vue des températures passées -->
    <div id="pastView" style="display: none;">
      <p>Veuillez entrer une ville pour visualiser la tendance des températures maximales moyennes entre 2010 et 2020.</p>
      <input id="cityInputPast" type="text" placeholder="Ex : Paris" />
      <button id="fetchBtnPast">Afficher la tendance</button>
      <h2 id="locationLabelPast"></h2>
      <canvas id="chart" width="800" height="400"></canvas>
    </div>

    <!-- Vue des prévisions météo -->
    <div id="forecastView" style="display: none;">
      <p>Veuillez entrer une ville pour voir la météo actuelle et les prévisions pour les 5 prochains jours.</p>
      <input id="cityInputForecast" type="text" placeholder="Ex : Paris" />
      <button id="fetchBtnForecast">Afficher la météo</button>
      <h2 id="locationLabelForecast"></h2>
      <div id="weatherInfo"></div>
    </div>
  </div>

  <!-- 引入 Chart.js CDN（使用全局变量方式） -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
  <script type="module" src="/WebDev101_Boyang_XU/scripts/climate.js"></script>
</BaseLayout>

